{{!-- burger-block.handlebars --}}
<li class="list-group-item">
    {{this.burger_name}}
    {{#if devoured}}
    {{else}}
    <button class="readyToEat" data-id="{{this.id}}" data-neweaten="{{devour}}">Consume!</button>
    {{/if}}
</li>