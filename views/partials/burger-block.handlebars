{{!-- burger-block.handlebars --}}
<li class="list-group-item">
    {{burger_name}}
    {{#if devoured}}
    {{else}}
    <button class="readyToEat" data-id="{{id}}" data-neweaten="{{devour}}">Consume!</button>
    {{/if}}
</li>